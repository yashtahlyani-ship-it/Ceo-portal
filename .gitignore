node_modules
dist
dist-ssr
*.local
.env
.env.*
!.env.example
.DS_Store
.vercel
.vite

.vercel
.env*

# The real team roster (names + working corporate emails).
# Gitignored deliberately: this repository is PUBLIC.
scripts/roster.json
